import 'dart:ui';

const Offset animateFromLeft = Offset(-1, 0);
const Offset animateFromRight = Offset(1, 0);
const Offset animateFromTop = Offset(0, -1);
const Offset animateFromBottom = Offset(0, 1);